bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind P source-file -F "#{@dirPrograms}/project-search.tmux.conf";
bind N source-file -F "#{@dirPrograms}/project-create.tmux.conf";
bind A source-file -F "#{@dirPrograms}/agent.tmux.conf";
bind O source-file -F "#{@dirPrograms}/agent-prompt.tmux.conf";
bind F source-file -F "#{@dirPrograms}/capture-context.tmux.conf";
