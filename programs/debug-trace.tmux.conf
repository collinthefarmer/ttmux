# toggle trace mode
if-shell -F "#{@debugTrace}" {
    set-option -s @debugTrace ""
    display-message "ttmux: trace OFF"
} {
    set-option -s @debugTrace "1"
    run-shell "echo '=== TRACE SESSION STARTED '$(date '+%H:%M:%S')' ===' > /tmp/ttmux-debug.log"
    display-message "ttmux: trace ON -> /tmp/ttmux-debug.log"
}
